import React from "react"; import { Input } from "../ui/input"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "../ui/select"; import { Search, SlidersHorizontal } from "lucide-react"; import { Button } from "../ui/button";  export default function SearchFilters({ filters, setFilters }) {   return (     <div className="bg-white rounded-[32px] border border-gray-100 shadow-sm p-6 mb-8">       <div className="flex items-center gap-2 mb-4">         <SlidersHorizontal className="w-5 h-5 text-teal-600" />         <h2 className="text-lg font-black text-gray-900 tracking-tighter">Buscar Espaços</h2>       </div>              <div className="grid grid-cols-1 md:grid-cols-3 gap-4">         {/* Busca por Cidade */}         <div className="relative">           <Search className="absolute left-4 top-1/2 transform -translate-y-1/2 w-4 h-4 text-gray-400" />           <Input             placeholder="Buscar por cidade..."             value={filters.city}             onChange={(e) => setFilters({ ...filters, city: e.target.value })}             className="pl-12 h-12 rounded-2xl border-gray-100 focus:ring-teal-500"           />         </div>          {/* Tipo de Espaço */}         <Select           value={filters.type}           onValueChange={(value) => setFilters({ ...filters, type: value })}         >           <SelectTrigger className="h-12 rounded-2xl border-gray-100">             <SelectValue placeholder="Tipo de espaço" />           </SelectTrigger>           <SelectContent className="rounded-2xl">             <SelectItem value="todos text-gray-400 font-bold">Todos os tipos</SelectItem>             <SelectItem value="piscina">Piscina</SelectItem>             <SelectItem value="chacara">Chácara</SelectItem>             <SelectItem value="salao_festas">Salão de Festas</SelectItem>             <SelectItem value="quadra_esportes">Quadra Esportiva</SelectItem>             <SelectItem value="espaco_eventos">Espaço para Eventos</SelectItem>             <SelectItem value="area_camping">área de Camping</SelectItem>             <SelectItem value="outro">Outro</SelectItem>           </SelectContent>         </Select>          {/* Faixa de Preço */}         <Select           value={filters.priceRange}           onValueChange={(value) => setFilters({ ...filters, priceRange: value })}         >           <SelectTrigger className="h-12 rounded-2xl border-gray-100">             <SelectValue placeholder="Faixa de preço" />           </SelectTrigger>           <SelectContent className="rounded-2xl">             <SelectItem value="all">Todos os preços</SelectItem>             <SelectItem value="0-200">Até R$ 200</SelectItem>             <SelectItem value="200-500">R$ 200 - R$ 500</SelectItem>             <SelectItem value="500-1000">R$ 500 - R$ 1000</SelectItem>             <SelectItem value="1000+">Acima de R$ 1000</SelectItem>           </SelectContent>         </Select>       </div>     </div>   ); } 