import React from "react"; import { Card, CardContent, CardHeader, CardTitle } from "../ui/card"; import { Star } from "lucide-react"; import { useQuery } from "@tanstack/react-query"; import { base44 } from "@/api/base44Client"; import { format } from "date-fns"; import { ptBR } from "date-fns/locale";  export default function ReviewsSection({ spaceId }) {   const { data: reviews = [] } = useQuery({     queryKey: ['reviews', spaceId],     queryFn: () => base44.entities.Review.filter({ space_id: spaceId }, '-created_date'),     enabled: !!spaceId   });    if (reviews.length === 0) {     return (       <div className="py-10 text-center border-t border-gray-100">         <p className="text-gray-400 italic">Este espaço ainda não recebeu avaliaçáµes. Seja o primeiro a viver esta experiáªncia!</p>       </div>     );   }    return (     <Card className="border-none shadow-sm rounded-[32px] bg-gray-50/50">       <CardHeader className="px-8 pt-8">         <div className="flex items-center gap-2">           <CardTitle className="text-2xl font-black text-gray-900 tracking-tighter">Avaliaçáµes</CardTitle>           <div className="bg-teal-500 text-white text-xs font-bold px-2 py-1 rounded-lg">             {reviews.length}           </div>         </div>       </CardHeader>       <CardContent className="space-y-6 p-8">         {reviews.map((review) => (           <div key={review.id} className="pb-6 border-b border-gray-200 last:border-0 last:pb-0">             <div className="flex items-start justify-between mb-3">               <div className="flex gap-3 items-center">                 <div className="w-10 h-10 rounded-full bg-black flex items-center justify-center text-white font-bold text-xs">                   {review.user_name ? review.user_name.substring(0, 2).toUpperCase() : "H"}                 </div>                 <div>                   <p className="font-bold text-gray-900 leading-none mb-1">{review.user_name || "Hóspede"}</p>                   <p className="text-xs text-gray-400 font-medium">                     {format(new Date(review.created_date), "MMMM 'de' yyyy", { locale: ptBR })}                   </p>                 </div>               </div>               <div className="flex items-center gap-0.5">                 {Array.from({ length: 5 }).map((_, i) => (                   <Star                      key={i}                      className={"w-3.5 h-3.5"}                    />                 ))}               </div>             </div>             {review.comment && (               <p className="text-gray-600 leading-relaxed text-sm pl-13">                 "{review.comment}"               </p>             )}           </div>         ))}       </CardContent>     </Card>   ); } 