import React from 'react'; import { MessageCircle, Sparkles } from 'lucide-react'; import { motion } from 'framer-motion'; import { useNavigate } from 'react-router-dom';  export default function LiaFloatingButton() {   const navigate = useNavigate();    return (     <div className="fixed bottom-8 right-8 z-[60]">       {/* Efeito de Ondas (Pulso) ao redor do botÃ£o */}                     <motion.button         whileHover={{ scale: 1.1 }}         whileTap={{ scale: 0.9 }}         onClick={() => navigate('/chat')}         className="relative flex items-center gap-3 bg-black text-white px-6 py-4 rounded-full shadow-2xl hover:bg-gray-800 transition-colors group"       >         <div className="relative">           <MessageCircle size={24} className="group-hover:rotate-12 transition-transform" />           <Sparkles size={12} className="absolute -top-1 -right-1 text-teal-400 " />         </div>         <div className="flex flex-col items-start leading-none">           <span className="text-[10px] font-black uppercase tracking-widest text-teal-400">DÃƒÂºvidas?</span>           <span className="text-sm font-bold">Falar com Lia</span>         </div>       </motion.button>     </div>   ); } 