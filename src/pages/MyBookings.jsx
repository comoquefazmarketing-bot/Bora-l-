import React, { useState } from "react"; import { format } from "date-fns"; import { ptBR } from "date-fns/locale"; import { Skeleton } from "../components/ui/skeleton"; import { motion } from "framer-motion"; import { Button } from "../components/ui/button"; import { Calendar, Clock, MapPin, CheckCircle2, XCircle, Clock3 } from "lucide-react"; import { Badge } from "../components/ui/badge";  export default function MyBookings() {   const [loading] = useState(false);      if (loading) {     return (       <div className="p-8">         <Skeleton className="h-12 w-48 mb-6" />         <div className="space-y-4">           {[1, 2, 3].map((i) => (             <Skeleton key={i} className="h-32 w-full rounded-xl" />           ))}         </div>       </div>     );   }    return (     <div className="min-h-screen bg-slate-50/50 p-4 md:p-8">       <div className="max-w-4xl mx-auto">         <header className="mb-8">           <h1 className="text-3xl font-bold text-slate-900">Meus Agendamentos</h1>           <p className="text-slate-500 mt-2">Gerencie suas reservas e experi̻ncias sensoriais.</p>         </header>          <div className="bg-white rounded-2xl border border-slate-100 p-12 text-center">           <div className="w-16 h-16 bg-slate-50 rounded-full flex items-center justify-center mx-auto mb-4">             <Calendar className="w-8 h-8 text-slate-300" />           </div>           <h3 className="text-lg font-semibold text-slate-900">Nenhuma reserva encontrada</h3>           <p className="text-slate-500 mt-2">Voc̻ ainda ṇo possui agendamentos ativos.</p>           <Button className="mt-6" onClick={() => window.location.href = '/'}>             Explorar Espa̤os           </Button>         </div>       </div>     </div>   ); }